# Implementation Plan: Generador de Rutinas\n\n**Branch**: `001-routine-generator` | **Date**: 2026-03-11 | **Spec**: [specs/001-routine-generator/spec.md](file:///Users/damiancomba/damiancom/lovable/sistema-medico-vue/specs/001-routine-generator/spec.md)\n**Input**: Feature specification from `/specs/001-routine-generator/spec.md`\n\n## Summary\n\nSe implementará la pantalla "Generador de Rutina" dentro de la sección del detalle del paciente (`PatientDetail.vue`). Dicha pantalla permitirá a los médicos construir dinámicamente rutinas faciales (AM/PM), generar un PDF utilizando `jspdf` y `html2canvas` para el paciente, y guardar dicho listado usando el endpoint existente de "apoyo domiciliario" mapeando las rutinas a una estructura JSON persistible.\n\n## Technical Context\n\n**Language/Version**: Vue 3 (Composition API / `<script setup>`), Quasar Framework\n**Primary Dependencies**: `jspdf`, `html2canvas`, `axios`\n**Storage**: Firebase/Backend as defined via `api.js` endpoints (`/home-supports`).\n**Testing**: Quasar Dev Server (Manual Verification via Browser UI).\n**Target Platform**: Web Browser\n**Project Type**: Single Page Application (SPA).\n\n## Constitution Check\n\n- Se utiliza español como lenguaje preferido (siguiendo reglas del usuario).\n- Se utiliza la forma más simple y eficiente posible, refactorizando un documento HTML crudo `RoutineGenerator.vue` en un componente Vue propiamente modular para evitar duplicaciones y código espagueti.\n- Integración utilizando las dependencias de generación PDF que ya constaban en el `package.json`.\n\n## Project Structure\n\n### Documentation (this feature)\n\n```text\nspecs/001-routine-generator/\n├── plan.md              # This file\n├── research.md          # Investigaciones respecto a persistencia y PDF\n├── data-model.md        # Entidades Reactivas del frontend\n└── spec.md              # Funcionalidad y requerimientos núcleo\n```\n\n### Source Code (repository root)\n\n```text\nsrc/\n├── components/\n│   └── [MODIFY] RoutineGenerator.vue   # Refactor total desde HTML de muestra a Componente Vue  \n├── pages/\n│   └── [MODIFY] PatientDetail.vue      # Enganchar el componente `<RoutineGenerator />` dentro de una nueva solapa\n└── services/\n    └── [MODIFY] api.js                 # Export/Definiciones adicionales de endpoints si fuera necesario\n```\n\n**Structure Decision**: Continuamos con la estructura existente de Quasar App (src/components, src/pages, src/services).\n\n## Proposed Changes\n\n### Componente `RoutineGenerator.vue`\n1. Reemplazar todo el contenido HTML estático con `<template>`, `<script setup>` y `<style scoped>`.\n2. Crear estado reactivo que almacene los diferentes pasos de la rutina AM/PM, nombre del paciente, notas y fecha.\n3. Añadir la lógica para poder agregar (`push`) y eliminar (`splice`) pasos.\n4. Integrar la función PDF invocando a `html2canvas(document.getElementById('routine-preview'))` y luego usando `jsPDF`.\n\n### Página `PatientDetail.vue`\n1. Añadir una nueva tab en los Paneles nombrada "Rutina" referenciando el modelo de tabs.\n2. Agrupar e instanciar ahí el componente `<RoutineGenerator />` pasándole posiblemente datos iniciales por `props` o exponiendo validadores.\n3. Implementar métodos para serializar/deserializar del backend los datos de la rutina en el hook `onMounted` y en el método de `guardarApoyo`.\n\n## Verification Plan\n\n### Manual Verification\n1. Ejecutar el servidor con `npm run dev` (actualmente corriendo en `npx quasar dev`).\n2. Navegar a la pantalla del detalle del paciente.\n3. Clickear en la tab de "Rutina"\n4. Añadir dos productos a la Rutina AM, y un producto a la Rutina PM.\n5. Hacer click en "Descargar PDF", verificando la consola que no haya errores de html2canvas y revisando que el PDF renderizado en las Descargas luzca idéntico al DOM.\n6. Hacer click en Guardar. Recargar la página e inspeccionar que los productos sigan ahí cargados (demostrando la integración exitosa de base de datos).\n
